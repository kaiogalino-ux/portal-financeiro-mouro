node_modules/
.next/
out/
build/
.env
.env*.local
*.tsbuildinfo
next-env.d.ts
coverage/

# Prisma Client é gerado por `npm run db:generate` — inclui binário do
# query engine (dezenas de MB, específico por plataforma), nunca versionar.
src/generated/
